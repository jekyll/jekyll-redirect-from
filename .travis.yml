language: ruby
before_script: bundle update
script: "script/cibuild"
sudo: false
cache: bundler

matrix:
  include:
    - JEKYLL_VERSION="~> 3.5"
    - JEKYLL_VERSION=""

rvm:
  - 2.3.0
  - 2.2
  - 2.1
  - 2.0
